# Telegram-бот для вивчення іноземних слів — вимоги (контекст для LLM)

> **Backlog/статус задач:**  **`/docs/tasksTrackingSystem.md`** (див. [Backlog та трекінг задач](#12-backlog-та-трекінг-задач)).

---

## 1) Огляд продукту

**Telegram-бот** допомагає користувачам вивчати слова іноземних мов.

### Основна цінність

- Користувач додає власні слова з перекладом.
- Бот зберігає їх у базі даних.
- У режимі навчання бот періодично надсилає **один випадковий запис (слово/переклад)** зі словника користувача.

### Цільова аудиторія

- Користувачі Telegram, які хочуть вивчати слова «малими порціями».

---

## 2) Терміни та визначення

- **Word** — елемент словника користувача: **слово** (`text`) + **переклад** (`translation`).
- **START_LEARNING mode** — режим, коли бот надсилає слова за розкладом.
- **Interval** — кількість хвилин між повідомленнями зі словами.
- **User** — Telegram user (ідентифікація: `telegram_user_id`).
- **FSM / User's status / user's state** — механізм, що визначає поточний контекст діалогу й те, **що саме бот очікує** (ввід тексту або натискання inline-кнопки).
- **DEFAULT (звичайний стан / status)** — стан «без активної операції», коли бот реагує на стандартні команди/кнопки.
- **Часове вікно** — певний час (після 9:00 і до 22:00), у який бот надсилає слова за розкладом  .
---

## 3) Обсяг (Scope)

### В межах поточного продукту (MVP)

- Додавання слів із перекладом.
- Видалення слів (по 1 слову) через inline-кнопки.
- Перегляд списку слів.
- Запуск/зупинка навчання.
- Вибір інтервалу надсилання через пресети.
- Інформація/інструкції про бота.

### Поза межами (поки що)

- Усе, що позначено як нереалізоване у `tasksTrackingSystem.md`.

---

## 4) Реалізовані функціональні вимоги (станом на зараз)

> **Важливо:** нижче перелічені вимоги вважаються вже реалізованими.

### 4.1 Управління словами

- **FR-01 (Add word):** `слово/переклад` → парсинг `/` → зберегти `text` і `translation`.
- **FR-02 (Remove word):** видалення через inline-кнопку → видаляється 1 запис.
- **FR-03 (List words):** показати список слів користувача.

### 4.2 Навчання

- **FR-04 (Start learning):** 1 випадковий запис кожні *N* хв, лише у часовому вікні.
- **FR-05 (Stop learning):** зупинити надсилання.
- **FR-06 (Set interval):** вибір пресету з inline-кнопок (хардкод у коді).

### 4.3 Інформація

- **FR-07 (Info/Instructions):** користувач може отримати інформацію/інструкції про бота через окремий пункт меню.

---

## 5) Поведінкові правила та сценарії (User flows)

## 5.0 Система станів (FSM)

> Бот використовує FSM: у кожен момент бот знає, що очікує від користувача (ввід або клік), і працює в одному з визначених станів.

### Стани

- **DEFAULT** — звичайний режим.
- **ADD_WORD** — бот очікує введення `слово/переклад`.
- **REMOVE_WORD** — бот показує inline-кнопки зі словами й очікує натискання.
- **SET_INTERVAL** — бот показує inline-кнопки з інтервалами й очікує натискання.
- **START_LEARN** — навчання активне (надсилання за розкладом у часовому вікні).

> Примітка: окремий стан **STOP_LEARN** наразі не використовується — натискання “Stop Learning” вимикає навчання і повертає користувача в **DEFAULT**.

### Переходи (коротко)

- **DEFAULT → ADD_WORD** (Add word)
- **DEFAULT → REMOVE_WORD** (Видалити слово)
- **DEFAULT → SET_INTERVAL** (Set interval)
- **DEFAULT → START_LEARN** (Start Learning)
- **START_LEARN → DEFAULT** (Stop Learning)
- **ADD_WORD / REMOVE_WORD / SET_INTERVAL → DEFAULT** після успішної дії або **Cancel**

### Неочікуваний ввід

- У **ADD_WORD**: показати приклад формату й лишитися в **ADD_WORD**.
- У **REMOVE_WORD** або **SET_INTERVAL**: якщо користувач надсилає текст — підказати натиснути inline-кнопку або **Cancel**.

(Опційно) **Cancel** у **ADD_WORD/REMOVE_WORD/SET_INTERVAL** повертає в **DEFAULT** без змін.

### 5.1 Додавання слова

**Сценарій:** користувач у **DEFAULT** натискає «Add word» → бот переходить у **ADD_WORD** і просить надіслати `слово/переклад` → користувач надсилає дані → бот парсить, зберігає у БД → підтверджує → повертає у **DEFAULT**.

**Правила парсингу та валідації:**

- Формат: `<word>/<translation>`.
- Дозволені пробіли навколо частин — їх потрібно обрізати (`trim`).
- Розділювач — **перший** символ `/` у рядку (split на 2 частини по першому `/`).
- Обидві частини мають бути непорожніми після `trim`.
- Якщо `/` відсутній або одна з частин порожня — бот просить надіслати коректний формат і показує приклад.
- Поведінка щодо дублікатів (дозволяти/забороняти) визначається у `tasksTrackingSystem.md` і має бути зафіксована однозначно.

**Приклад:**

- User: `apple/яблуко`
- Bot: `✅ The word  **apple** with translation  **яблуко** has been added. You can add more!`

### 5.2 Видалення слова

**Сценарій:** користувач у **DEFAULT** натискає «Видалити слово» → бот переходить у **REMOVE_WORD** і показує слова inline-кнопками → користувач натискає кнопку зі словом → бот видаляє **один** запис → підтверджує → повертає у **DEFAULT**.

- Якщо слово не знайдено (наприклад, кнопка застаріла або слово вже видалене) — бот повідомляє, що такого слова немає.

**Приклад:**

- User натискає: `➖️ Remove word`
- Bot: `Please, chose the word You want to delete :` (inline-кнопки: `apple`, `orange`, ...)
- User натискає: `apple`
- Bot: `✔️ Word apple has been deleting successfully`

### 5.3 Список слів

**Сценарій:** користувач просить список → бот показує всі слова користувача.

- Якщо список порожній — бот пропонує додати перше слово.
- Слова відображаються у **алфавітному порядку**.

**Приклад:**

- User: `📜️️ Show all words`
- Bot:
    - `apple — яблуко`
    - `orange — апельсин`

### 5.4 Початок навчання

**Сценарій:** користувач запускає навчання → бот підтверджує → вмикається стан **START_LEARN** → надсилання йде за обраним інтервалом **у часовому вікні**.

- Якщо у користувача немає слів — бот не запускає навчання і просить додати слова.

**Приклад (повідомлення-підтвердження):**

- Bot: `▶️ Навчання увімкнено. Інтервал: 1 год. Час надсилання: 09:00–22:00.`

### 5.5 Зупинка навчання

**Сценарій:** користувач натискає «Stop Learning» → бот вимикає навчання → повертає у **DEFAULT**.

**Приклад:**

- Bot: `⏸️ Навчання зупинено.`

### 5.6 Вибір інтервалу

**Сценарій:** користувач натискає «Set interval» → бот переходить у **SET_INTERVAL** → показує пресети inline-кнопками → користувач обирає пресет → бот зберігає значення та застосовує для наступних надсилань → повертає у **DEFAULT**.

- Інтервал **не вводиться вручну**.
- Пресети **хардкодні в коді** (наприклад: 15 хв, 30 хв, 1 год, 2 год, …).

**Приклад:**

- User натискає: `Set interval`
- Bot: `Оберіть інтервал:` (inline-кнопки: `15 хв`, `30 хв`, `1 год`, `2 год`, ...)
- User натискає: `1 год`
- Bot: `⏱️ Інтервал встановлено: 1 год.`

### 5.7 Інформація про бота

**Сценарій:** користувач обирає «/instructions» → бот показує короткий опис і доступні дії.

---

## 6) Інтерфейс у Telegram

> Конкретні команди/кнопки можуть відрізнятися залежно від реалізації, але **функції** мають зберігатися.

### 6.1 Меню (приклад)

- `➕ Add word`
- `📜️️ Show all words`
- `➖️ Remove word`
- `🟢️ Start learning`
- `🟥️ Stop learn`
- `/set_interval`
- `/instructions`

### 6.2 Вимоги до UX

- Повідомлення мають бути короткими й однозначними.
- Після дії завжди має бути підтвердження (успіх/помилка).
- Емодзі — помірно.

---

## 7) Дані та зберігання

### 7.1 Загальна характеристика БД
- Дані зберігаються у БД.

### 7.2 Правила даних

- Дані одного користувача **не змішуються** з іншими.
- «Випадковий запис» обирається **лише** зі словника конкретного користувача.

---

## 8) Планувальник (scheduler) для навчання

### 8.1 Поведінка та очікування

- Надсилання слів обмежене часовим вікном (зараз це забезпечено cron-job’ом).
- **перше повідомлення дня** надсилається у першу дозволену годину часового вікна, наприклад о **09:00**  (якщо навчання активне).
- Якщо наступне спрацювання виходить за межі вікна (після 22:00) — надсилання не виконується до наступного доступного часу у вікні.
- Система має запускати періодичну задачу для кожного користувача з активним навчанням.
- Після зупинки навчання задача має гарантовано не виконуватися.

### 8.2 Поведінка після перезапуску бота

- `is_learning_active` і `interval_minutes` зберігаються в БД.
- Після рестарту сервіс має відновити планувальник для активних користувачів.

---

## 9) Нефункціональні вимоги

### 9.1 Надійність

- Не надсилати повідомлення частіше, ніж задано інтервалом.
- Обробляти помилки Telegram API (таймаути, rate limits) без падіння сервісу.

### 9.2 Безпека та приватність

- Валідовувати ввід і коректно обробляти спеціальні символи (щоб уникати інʼєкцій у БД/логах).
- Зберігати мінімально необхідні дані.
- Не логувати персональні дані понад необхідне (мінімум: `telegram_user_id`).

### 9.3 Спостережуваність

- Логи ключових подій: додавання/видалення слова, старт/стоп навчання, зміна інтервалу, помилки надсилання.

---

## 10) Обмеження та припущення

- Telegram — єдиний клієнтський канал.
- Один користувач може мати багато слів.
- Інтервал задається у хвилинах (через пресети).
- Повідомлення під час навчання надсилаються лише у часовому вікні (див. FR-04).

---

## 11) Acceptance Criteria (критерії приймання)

- **AC-01:** Доданий запис `слово/переклад` з’являється у списку після додавання; `text` і `translation` збережені окремо та відображаються коректно.
- **AC-02:** Видалений запис зникає зі списку і більше не надсилається під час навчання.
- **AC-03:** Навчання надсилає рівно **один** випадковий запис кожні *N* хвилин **у часовому вікні**; поза цим вікном повідомлення не надсилаються.
- **AC-04:** Після зупинки навчання повідомлення не надходять.
- **AC-05:** Обраний інтервал впливає на наступні надсилання.
- **AC-06:** Список слів відображається у алфавітному порядку.

---

## 12) Backlog та трекінг задач

Нереалізовані вимоги та задачі постійно оновлюються у файлі:

- **`/docs/tasksTrackingSystem.md`**

### Правила для `tasksTrackingSystem.md`

- Це спрощена система керування завданнями.
- **Реалізовані** задачі — **закреслені**.
- **Нереалізовані** задачі — **не закреслені** або знаходяться в секції **“CURRENT TASKS”**.

> **Джерело істини щодо того, що робити далі:** `tasksTrackingSystem.md`.

---

## 13) Інструкції для LLM (як працювати з цим проєктом)

▪ Спершу звіряйся з розділом [Реалізовані функціональні вимоги](#4-реалізовані-функціональні-вимоги-станом-на-зараз).
▪ Усе «що робити далі» бери з `tasksTrackingSystem.md` (backlog) і синхронізуй зміни з цим файлом вимог.
▪ Нова фіча = короткий user story + acceptance criteria + запис у `tasksTrackingSystem.md`.
▪ Принцип: **одна дія користувача → однозначна відповідь бота**.

---

## 14) Журнал змін (опційно)

- **v1.0** — зафіксовано поточні реалізовані вимоги та правила трекінгу задач.